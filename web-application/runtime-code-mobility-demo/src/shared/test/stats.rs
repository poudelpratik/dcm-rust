// pub type Number = u64;
//
// pub fn mean(data: &[Number]) -> Number {
//     data.iter().sum::<Number>() / data.len() as Number
// }
//
// pub fn variance(data: &[Number]) -> Number {
//     let data_mean = mean(data);
//     data.iter().map(|&x| (x - data_mean).pow(2)).sum::<Number>() / data.len() as Number
// }
